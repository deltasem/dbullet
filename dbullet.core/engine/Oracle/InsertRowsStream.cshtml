@{string[] props = (string[])Model[1];}
insert into @Model[0] (
@for(int i = 0; i < props.Length; i++){
@props[i]@(i != props.Length-1?", ":"")
}) values(
@for(int i = 0; i < props.Length; i++){
@:@@@props[i]@(i != props.Length - 1 ? ", " : "")
});