@{
	var table = Model[0] as string; 
	var column = Model[1] as string;
}
select count(*) from user_tab_columns where initcap(table_name) = initcap('@table') and initcap(column_name) = initcap('@column')